#!/bin/sh
# Work around rustup channel probing in Solana CLI 3.1.x on macOS. The custom
# SBF toolchain is installed by cargo-build-sbf; `rustup run` selects it
# without asking rustup to contact the channel server.
exec rustup run 1.89.0-sbpf-solana-v1.52 rustc "$@"
